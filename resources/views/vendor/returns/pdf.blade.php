<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Product Returns Export</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 10px;
        }
        .vendor-info {
            text-align: center;
            margin-bottom: 20px;
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th {
            background-color: #f8f9fa;
            color: #333;
            font-weight: bold;
            padding: 10px;
            text-align: left;
            border: 1px solid #ddd;
        }
        td {
            padding: 8px;
            border: 1px solid #ddd;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .badge {
            padding: 3px 8px;
            border-radius: 3px;
            font-size: 10px;
            font-weight: bold;
        }
        .badge-warning { background-color: #ffc107; color: #000; }
        .badge-info { background-color: #17a2b8; color: #fff; }
        .badge-success { background-color: #28a745; color: #fff; }
        .badge-danger { background-color: #dc3545; color: #fff; }
        .badge-primary { background-color: #007bff; color: #fff; }
        .badge-secondary { background-color: #6c757d; color: #fff; }
        .footer {
            margin-top: 30px;
            text-align: center;
            color: #666;
            font-size: 10px;
        }
    </style>
</head>
<body>
    <h1>Product Returns Report</h1>
    <div class="vendor-info">
        <strong>{{ $vendor->store_name ?? $vendor->business_name }}</strong><br>
        Generated on: {{ date('d M Y, h:i A') }}
    </div>
    
    <table>
        <thead>
            <tr>
                <th>Return ID</th>
                <th>Invoice #</th>
                <th>Customer</th>
                <th>Type</th>
                <th>Reason</th>
                <th>Amount</th>
                <th>Status</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            @forelse($returns as $return)
            <tr>
                <td>#{{ $return->id }}</td>
                <td>{{ $return->invoice->invoice_number ?? 'N/A' }}</td>
                <td>
                    @if($return->user)
                        {{ $return->user->name }}
                    @elseif($return->vendorCustomer)
                        {{ $return->vendorCustomer->name }}
                    @else
                        N/A
                    @endif
                </td>
                <td>
                    @if($return->user)
                        Web
                    @elseif($return->vendorCustomer)
                        App
                    @else
                        N/A
                    @endif
                </td>
                <td>{{ Str::limit($return->return_reason, 30) }}</td>
                <td>₹{{ number_format($return->refund_amount, 2) }}</td>
                <td>
                    @php
                        $statusColors = [
                            'pending' => 'warning',
                            'under_review' => 'info',
                            'approved' => 'success',
                            'rejected' => 'danger',
                            'pickup_scheduled' => 'primary',
                            'picked_up' => 'primary',
                            'received' => 'info',
                            'inspected' => 'info',
                            'refund_processing' => 'warning',
                            'completed' => 'success',
                            'cancelled' => 'secondary',
                        ];
                        $statusColor = $statusColors[$return->status] ?? 'secondary';
                    @endphp
                    <span class="badge badge-{{ $statusColor }}">
                        {{ ucfirst(str_replace('_', ' ', $return->status)) }}
                    </span>
                </td>
                <td>{{ $return->created_at->format('d M Y') }}</td>
            </tr>
            @empty
            <tr>
                <td colspan="8" style="text-align: center; padding: 20px; color: #999;">
                    No returns found
                </td>
            </tr>
            @endforelse
        </tbody>
    </table>
    
    <div class="footer">
        <p>Total Returns: {{ $returns->count() }} | Generated by {{ config('app.name') }}</p>
    </div>
</body>
</html>
